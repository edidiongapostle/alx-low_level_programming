#include <stdio.h>

/**
 * main - Entry point
 * A program that prints a line of code using puts
 * Return: 0
 */
init main(void)
{
	puts("Programming is like building a multilingual puzzle");
	return (0);
}
